<Page x:Class="GamingWPFGUI2.Orders"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GamingWPFGUI2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Orders">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="5">
                <ListBox x:Name="ListBoxCustomers" Background="Beige"></ListBox>
            </Grid>
            <Grid Grid.Row="1" Margin="5">
                <ListBox x:Name="ListBoxGames" Background="Beige"></ListBox>
            </Grid>
        </Grid>
        <Grid Grid.Column="1">
            <ListBox x:Name="ListBoxOrders" Background="MintCream"></ListBox>
        </Grid>
        
    </Grid>
</Page>
